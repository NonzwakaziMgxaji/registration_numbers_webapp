<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/registration_number.css">
    <title>Registration_numbers_webapp</title>
</head>

<body>
    <div class="container">
        <section class="regNum">
            <h1>REGISTRATION NUMBERS WEBAPP</h1>

            <form action="/reg_numbers" method="POST">

                {{#if messages.warning}}
                <div class="errorMsg" id="errorMsgID">{{messages.warning}}</div><br><br>
                {{/if}}

                {{#if messages.feedback}}
                <div class="displayFeedback" id="errorMsgID">{{messages.feedback}}</div><br><br>
                {{/if}}

                <div class="RegNum">
                    <label for="regN">Registration Number</label>
                    <input class="RegTextbox" type="text" id="regText" name="regText"
                        placeholder="FORMAT: CA 123-456 / CY 123456/ CL 123 456">
                </div><br>

                <input class="addBtn" type="submit" value="ADD"><br><br>
            </form>

            <form action="/selectTown" method="POST">
                <div class="row inputs">
                    <input class="radio" type="radio" name="town" id="town" value="CA">
                    <label class="radioName">Cape Town</label>
                    <input class="radio" type="radio" name="town" id="town" value="CY">
                    <label class="radioName">Bellville</label>
                    <input class="radio" type="radio" name="town" id="town" value="CK">
                    <label class="radioName">Malmesbury</label>
                    <input class="radio" type="radio" name="town" id="town" value="CL">
                    <label class="radioName">Stellenbosch</label>
                </div><br>

                <input class="showBtn" type="submit" value="SHOW">
            </form>

            <div class="formsss">
                <form action="/showAllTowns" method="GET">
                    <input class="showAllBtn" type="submit" value="SHOW ALL">
                </form>

                <form action="/reset" method="POST">
                    <input class="resetBtn" type="submit" value="RESET">
                </form>
            </div><br><br>

            {{#each allReg}}
            <p class="col-12 showRegNum" id="showRegNumID">{{this.regnum}}</p>
            {{/each}}
        </section>
    </div>
</body>

</html>